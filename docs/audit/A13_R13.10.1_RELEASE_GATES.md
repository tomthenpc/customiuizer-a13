# A13 r13.10.1 Release Gate Verification

## 审计元数据

| 字段 | 值 |
|------|-----|
| Base SHA | `a9f85a71219c2d2b59315f207e9f8725d9a0a1f4` |
| Gate 执行日期 | 2026-08-06 |
| 任务 | `VERIFY-A13-R13.10.1-RELEASE-GATES` |

## 版本身份

| 字段 | 值 | 来源 |
|------|-----|------|
| versionCode | 135 | `app/build.gradle.kts:23` |
| versionName | r13.10.1 | `app/build.gradle.kts:24` |
| applicationId | tv.withaibuild.customiuizer.r13 | Manifest |
| minSdk | 33 | Manifest |
| targetSdk | 34 | Manifest |
| compileSdk | 36 | `app/build.gradle.kts:39` |
| ABI | arm64-v8a | `app/build.gradle.kts:65` |
| release debuggable | false | Manifest 无 `android:debuggable` |
| release minify | true | `app/build.gradle.kts:74` |
| release shrinkResources | true | `app/build.gradle.kts:75` |

### 陈旧引用检查

- `r13.10.0` 仅保留在 `CHANGELOG.md:30` 历史标题。
- `r13.10.1` / 135 出现在 `app/build.gradle.kts`、`CHANGELOG.md:3`、`README.md:36`、`README_EN.md:36`。
- 无活跃旧版本引用。

## 任务图安全检查

### 已确认存在任务

- `compileReleaseKotlin` ✓
- `compileReleaseJavaWithJavac` ✓
- `testReleaseUnitTest` ✓
- `lintRelease` ✓
- `minifyReleaseWithR8` ✓
- `signingReport` ✓

### 计划执行任务的 dry-run

命令：

```powershell
.\gradlew.bat :app:compileReleaseKotlin :app:compileReleaseJavaWithJavac :app:testReleaseUnitTest :app:lintRelease :app:minifyReleaseWithR8 --dependency-verification=strict --dry-run --console=plain
```

结论：dry-run 任务图未包含 `assemble`、`package`、`bundle`、`install`、`publish`、`packageRelease`、`assembleRelease`、`bundleRelease`、`signRelease`、`validateSigningRelease`。

### Vital Lint 任务图

- `:app:lintVitalRelease` dry-run 包含 `compileReleaseKotlin`、`compileReleaseJavaWithJavac`、`packageReleaseResources`（资源打包，非 APK）。
- 为避开完整 `lintVitalRelease` 生命周期，改为执行：
  - `:app:lintVitalAnalyzeRelease`
  - `:app:lintVitalReportRelease`

dry-run 显示它们不拉入任何 APK/package/sign 任务。

## Python 与通用静态门禁

| 检查 | 命令 | 结果 |
|------|------|------|
| Python 编译 | `python -m compileall tools` | 通过 |
| Python 测试 | `python -m unittest discover -s tools/tests -p "test_*.py"` | 902 通过（2 skipped） |
| Invariants | `python tools/check-invariants.py` | 通过（170 files, no violations） |
| Compat contracts | `python tools/check-compat-contracts.py` | OK |
| Hook parity | `python tools/check_hook_contract_parity.py` | OK |
| 完整门禁 | `python tools/verify.py full` | 通过 |
| 源码危害 | `python tools/source_hazard_scan.py --path app/src/main/java` | 0 reviewed, 0 new |
| printStackTrace | `Get-ChildItem app/src/main/java -Recurse -Include *.kt,*.java \| Select-String -SimpleMatch ".printStackTrace("` | 无输出 |
| diff 检查 | `git diff --check` | 无报错 |

## Release 编译、测试、Lint 与 R8

实际执行命令：

```powershell
.\gradlew.bat :app:compileReleaseKotlin :app:compileReleaseJavaWithJavac :app:testReleaseUnitTest :app:lintRelease :app:minifyReleaseWithR8 --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain --stacktrace
```

结果：

| 任务 | 结果 |
|------|------|
| `compileReleaseKotlin` | BUILD SUCCESSFUL |
| `compileReleaseJavaWithJavac` | BUILD SUCCESSFUL |
| `testReleaseUnitTest` | BUILD SUCCESSFUL |
| `lintRelease` | BUILD SUCCESSFUL |
| `minifyReleaseWithR8` | BUILD SUCCESSFUL |
| strict dependency verification | 通过 |

Vital Lint 执行：

```powershell
.\gradlew.bat :app:lintVitalAnalyzeRelease :app:lintVitalReportRelease --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain --stacktrace
```

结果：BUILD SUCCESSFUL。

## R8 证据

### minifyReleaseWithR8 中间产物

| 文件 | 大小 | 说明 |
|------|------|------|
| `app/build/outputs/mapping/release/configuration.txt` | 存在 | R8 配置 |
| `app/build/outputs/mapping/release/mapping.txt` | 存在 | mapping |
| `app/build/outputs/mapping/release/resources.txt` | 存在 | 资源映射 |
| `app/build/outputs/mapping/release/seeds.txt` | 存在 | 种子 |
| `app/build/outputs/mapping/release/usage.txt` | 存在 | usage |

- 不存在非空 `missing_rules.txt`。
- 日志中无 `Missing class`、`R8:.*error`、`Compilation failed` 等实质错误。
- `minifyReleaseWithR8` 为 `executed`（非 UP-TO-DATE）。
- R8 警告分类：仅编译器/ktlint 级别警告（如 unnecessary cast、deprecated API），无 missing class 或 R8 错误。

## Release Manifest 与 Xposed 静态合同

### 合并 Manifest（`app/build/intermediates/packaged_manifests/release/.../AndroidManifest.xml`）

| 检查 | 结果 |
|------|------|
| `package` | `tv.withaibuild.customiuizer.r13` |
| `android:versionCode` | `135` |
| `android:versionName` | `r13.10.1` |
| `android:debuggable="true"` | 不存在 |
| `android:testOnly="true"` | 不存在 |
| `minSdkVersion` | `33` |
| `targetSdkVersion` | `34` |

### Xposed 元数据

| 文件 | 状态 |
|------|------|
| `app/src/main/resources/META-INF/xposed/module.prop` | 存在 |
| `app/src/main/resources/META-INF/xposed/java_init.list` | 存在 |
| `app/src/main/resources/META-INF/xposed/scope.list` | 存在 |

内容：

- `minApiVersion=101`
- `targetApiVersion=102`
- `staticScope=false`
- `java_init.list`：`tv.withaibuild.customiuizer.MainModule`

- 无 debug 专用入口；libxposed metadata 与生产配置一致。

## 签名输入预检

### 签名配置解析

| 项 | 状态 |
|----|------|
| signing properties path resolved | false |
| properties file exists | false |
| storeFile key present | false |
| storePassword key present | false |
| keyAlias key present | false |
| keyPassword key present | false |
| keystore file exists | false |

### signingReport 输出

```text
Variant: debug
Config: debug
Store: C:\Users\tv\.android\debug.keystore
Alias: AndroidDebugKey
SHA-256: 6F:74:1D:B8:D7:8C:CB:5B:CB:00:51:2D:6A:DE:F2:D3:8A:F3:54:A5:A1:38:C0:6B:88:AF:26:66:C2:D0:98:B0
```

Release variant 未出现在 `signingReport` 中，因为 `app/build.gradle.kts` 仅在存在外部签名 properties 时才配置 `signingConfig`。

### 历史签名基线

- 已记录正式 A13 证书 SHA-256：`15CE32F03E4D8E62DF9390F77431862E59BF2CF95CD5A72F0C7330CDFCCA2934`（来自 `tasks/completed/REL-A13-r13.10.0-private-signed.md`）。
- 当前无法解析 release 签名输入，因此无法与该基线比较。

## APK/AAB/APKS 生成证明

任务前后 APK 清单一致，无新增、无更新时间：

| 相对路径 | 长度 | SHA-256（片段） |
|----------|------|-----------------|
| `app/build/outputs/apk/debug/CustoMIUIzer-A13-r13.9.2-debug.apk` | 12784192 | 750B... |
| `app/build/outputs/apk/release/CustoMIUIzer-A13-r13.10.0.apk` | 2925230 | 74BB... |
| `CustoMIUIzer-A13-r13.2.2-*.apk` | 多个 | 旧构建产物 |

无 `*.aab`、无 `*.apks`。

## 禁止命令确认

| 命令类型 | 是否执行 |
|----------|----------|
| `assemble*` | 否 |
| `package*` | 否 |
| `bundle*` | 否 |
| `install*` | 否 |
| `publish*` | 否 |
| `sign*` | 否 |

## 最终决策

```text
BLOCKED_SIGNING_INPUT
```

理由：

- 所有静态编译、测试、Lint、R8、Manifest、Xposed 元数据和依赖验证均通过；
- 未生成新的 APK/AAB/APKS；
- 仓库未修改生产文件；
- **但 release 签名 properties/keystore 未在当前环境中解析**，`signingReport` 仅显示 debug 配置，无法验证 release 签名身份与历史正式证书基线 `15CE32F...` 一致。

因此不能标记 `RELEASE_GATES_VERIFIED`。在提供外部 A13 专用签名配置并确认证书 SHA-256 与基线一致后，方可进行正式签名 Release APK 构建。

## 输出清单

- `docs/audit/A13_R13.10.1_RELEASE_GATES.md`（本文件）
- 未新增/提交任何 APK、AAB、mapping、签名配置
