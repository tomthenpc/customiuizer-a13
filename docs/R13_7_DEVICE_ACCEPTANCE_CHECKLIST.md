# R13.7 实机验收清单

适用分支：`codex/r13.7-final-audit`

本清单只用于 MIUI 14 / Android 13 实机验收。静态构建、Lint、R8、签名和 CI 通过不能替代本清单。每项状态只能填写：

```text
PASS
FAIL
NOT TESTED
NOT APPLICABLE
```

测试前记录设备、ROM、LSPosed/Vector 版本、模块版本、配置来源和测试提交。不得在没有对应证据时填写 `PASS`。

## A. 基础加载

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| 模块 APK 使用正式证书安装，包名为 `tv.withaibuild.customiuizer.r13` | `<状态>` | |
| LSPosed/Vector 作用域与预期一致 | `<状态>` | |
| 日志包含当前版本在目标进程的模块加载行 | `<状态>` | |
| `system_server`、SystemUI、Launcher 启动无模块因果崩溃 | `<状态>` | |
| 设置应用首页、子页、搜索可进入 | `<状态>` | |

## B. K11 回调与生命周期

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| 偏好变更回调只执行一次，不重复副作用 | `<状态>` | |
| Launcher 重命名只更新唯一匹配项 | `<状态>` | |
| SystemUI 重建后旧 Receiver/Observer/Handler 不再响应 | `<状态>` | |
| 重复注册替换时不误注销其他 owner | `<状态>` | |
| 故意触发兼容失败时日志不持续刷屏 | `<状态>` | |

## C. K12 设备监控

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| 电池详情、设备温度关闭时无 ticker | `<状态>` | |
| 模式 1/2/3 显示内容与既有语义一致 | `<状态>` | |
| 熄屏后停止周期任务，亮屏仅立即刷新一次 | `<状态>` | |
| 连续读取失败退避有上限，成功后恢复正常间隔 | `<状态>` | |
| SystemUI 重建后旧 controller 不再更新 View | `<状态>` | |

## D. K13 设置列表与图标

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| 快速滚动、搜索和返回时无越界或错位 | `<状态>` | |
| 复用 ImageView 不显示旧 item 图标 | `<状态>` | |
| 快速连续搜索时旧结果不覆盖新结果 | `<状态>` | |
| 应用安装、卸载、多用户场景无错误缓存共享 | `<状态>` | |
| 长时间滚动无持续队列增长或明显卡顿 | `<状态>` | |

## E. K14 语言与搜索

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| Android 13 应用语言切换通过 LocaleManager 生效 | `<状态>` | |
| `auto` 清空 application locales 并跟随系统 | `<状态>` | |
| 冷启动、旋转、返回栈和 Fragment 重建语言一致 | `<状态>` | |
| 搜索返回后开关状态刷新正确 | `<状态>` | |
| 搜索排序、高亮和路由语义未变化 | `<状态>` | |

## F. K15 AudioVisualizer

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| 播放、暂停、切歌、锁屏/解锁时显示与旧语义一致 | `<状态>` | |
| 熄屏、不可见、detach 后停止帧回调 | `<状态>` | |
| 重新 attach 不产生重复 Visualizer 链接 | `<状态>` | |
| 快速切歌时旧 Palette 不覆盖新封面颜色 | `<状态>` | |
| 30 分钟播放无崩溃、持续线程/队列增长或明显掉帧 | `<状态>` | |

## G. K16 锁屏专辑图

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| 快速切歌只显示最新专辑图 | `<状态>` | |
| blur、缩放、灰度和 WallpaperColors 视觉语义未变化 | `<状态>` | |
| 熄屏和 owner detach 后旧结果不回写 | `<状态>` | |
| 重复相同封面缓存命中且不同来源不串图 | `<状态>` | |
| OOM、取消或任务拒绝不拖垮 SystemUI | `<状态>` | |

## H. K17 综合冒烟

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| System 相关已启用功能按既有条件工作 | `<状态>` | |
| SystemUI 相关已启用功能按既有顺序工作 | `<状态>` | |
| Launcher 相关已启用功能按既有顺序工作 | `<状态>` | |
| `system_server`、SystemUI、Launcher 无缺失 facade/R8 入口错误 | `<状态>` | |
| API 101 运行基线与 API 102 编译元数据一致 | `<状态>` | |

## I. 30 分钟稳定性观察

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| 观察窗口内无模块因果 crash、ANR、fatal、watchdog | `<状态>` | |
| SystemUI、Launcher、system_server PID 未异常变化 | `<状态>` | |
| 日志无同一回调持续刷屏 | `<状态>` | |
| 功能关闭时无对应持续任务 | `<状态>` | |
| 主观流畅度和耗电异常已记录但不冒充定量结论 | `<状态>` | |

## J. 日志结论

| 项目 | 状态 | 证据/备注 |
|---|---|---|
| 已保存完整 logcat | `<状态>` | |
| 已保存 LSPosed/Vector/CustoMIUIzer 加载筛选日志 | `<状态>` | |
| 已保存 crash/ANR/fatal/watchdog 摘要 | `<状态>` | |
| 包名出现与模块因果问题已区分 | `<状态>` | |
| 最终结论和未测项已写入 K7 报告 | `<状态>` | |

## 验收边界

- 任一 P0/P1 模块因果问题：本候选不可合并、不可发布。
- `NOT TESTED` 不能折算为通过。
- K7、K12、K15、K16 在本清单完成前继续保持“待实机验证”。
- 完成本清单也不自动授权合并 `main`、创建 tag 或发布 Release。
