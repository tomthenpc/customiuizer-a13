# r13.8.5

米客 A13 面向 MIUI 14 / Android 13 的本地签名候选版本。

## 版本信息

- applicationId：`tv.withaibuild.customiuizer.r13`
- versionName：`r13.8.5`
- versionCode：`130`
- ABI：`arm64-v8a`
- compileSdk / minSdk / targetSdk：`36` / `33` / `34`
- libxposed：`minApiVersion=101`、`targetApiVersion=102`、`staticScope=false`
- 源分支：`release/r13.8.5`
- 源提交：`0ffa39a`
- 构建提交：`0ffa39a`

## 主要变化

- 以 `fix/r13.8.3-ui-text-inheritance-and-about-wrap` 为基线，将 A13 维护、Catalog 扩展、安装证据与文本修复分支的有效提交统一到单一分支 `release/r13.8.5`；
- 更新 `release-manifest.json`、`README`、`CHANGELOG`、构建与测试文档中的活动版本；修正 `compileSdk` 不一致与过期 `r13.7.1` draft；
- 明确 `certificateDnNote` 中的 A14 名称为历史证书标签，未更换签名者，保留用于 A13 安装升级兼容；
- 修正 CI 分支触发为 `release/**` 并固化 `audit-system-migration.py` 基线为稳定 SHA。
- 性能优化：限制图标缓存、规范 MainApplication 内存回调、按需懒加载 Catalog 与 Resolver、状态栏/通知/网络速度/电池/时钟/Launcher 高频路径减少参数复制与 Regex/String.format 分配。

## 状态

**本地签名候选版本，待实机和 LSPosed 日志验证。** 不创建 Git tag 或 GitHub Release。

## 发布门禁

- `check-invariants.py`：PASS（136 个文件，0 违规）
- `audit-system-migration.py`：PASS（124/124 基线方法解析，119/119 MainModule 直连调用，0 facade 调用）
- `audit-architecture.py`：PASS（25 features，29 diagnostic IDs，27 schema entries，27 catalog preference keys）
- `audit-prefs.py`：PASS（schema 与 code/xml 对齐，无重复 key；大量旧版 preference key 在 schema 外为已知状态）
- `audit-canary-sequence.py`：PASS（顺序保持，条件已迁移到 catalog，无批量安装残留）
- `audit-catalog-contracts.py`：PASS（8 + 6 + 6 + 5 contracts，HookRequirement 模型一致，无空/重复候选，签名匹配）
- `:app:test`：PASS
- `:app:lintDebug`：PASS
- `:app:lintRelease`：PASS
- `:app:lintVitalRelease`：SKIPPED（当前配置无 vital 变体验证）
- `:app:assembleDebug`：PASS
- `:app:minifyReleaseWithR8`：PASS
- `:app:assembleRelease`：PASS
- zipalign：PASS（构建期 `packageRelease` 自动完成对齐）
- APK Signature Scheme v2：PASS
- Legacy Xposed API DEX 扫描：PASS（APK 中未检出 `de/robv/android/xposed`）

## APK

- 文件：`CustoMIUIzer-A13-r13.8.5.apk`
- 大小：`2,830,726` bytes
- SHA-256：`98355766E3D13EDBFCBABD0021EFEF574A2427D19EB34A6A2CF07F394A309FE1`
- 证书 SHA-256：`C0EFF2DC4E662717195490DA78B12A984C6F2E6BD38ACF4EDAD14D53E3D22E70`
- 证书 DN：`CN=CustoMIUIzer A14, OU=Release, O=tomthenpc, C=CN`
- applicationId：`tv.withaibuild.customiuizer.r13`
- versionCode：`130`
- versionName：`r13.8.5`
- minSdk：`33`
- targetSdk：`34`
- compileSdk：`36`
- Xposed scope：`android`、`system`、`com.android.systemui`、`com.miui.home`、`com.mi.android.globallauncher` 等 15 个作用域
- `module.prop`：`minApiVersion=101`，`targetApiVersion=102`，`staticScope=false`
- `java_init.list`：`tv.withaibuild.customiuizer.MainModule`

## 验证边界

本版本为本地签名候选；编译、Lint、R8 和签名属于静态验证，不能代替设备与 ROM 行为验证。遇到兼容问题时，请先关闭对应功能并提交完整 LSPosed 日志。

---

# r13.8.5 (English)

CustoMIUIzer A13 locally-signed candidate for MIUI 14 / Android 13.

## Version

- applicationId: `tv.withaibuild.customiuizer.r13`
- versionName: `r13.8.5`
- versionCode: `130`
- ABI: `arm64-v8a`
- compileSdk / minSdk / targetSdk: `36` / `33` / `34`
- libxposed: `minApiVersion=101`, `targetApiVersion=102`, `staticScope=false`
- source branch: `release/r13.8.5`
- source commit: `0ffa39a`
- build commit: `0ffa39a`

## Highlights

- Consolidated effective commits from A13 maintenance, Catalog expansion, install-evidence, and UI text branches into `release/r13.8.5`, using `fix/r13.8.3-ui-text-inheritance-and-about-wrap` as the baseline;
- Updated the active version in `release-manifest.json`, README, CHANGELOG, build and test docs; fixed `compileSdk` mismatch and stale `r13.7.1` draft;
- Clarified the `certificateDnNote` A14 label is a historical certificate name; the signing key was not changed, and it is kept for A13 upgrade compatibility;
- Updated CI to trigger on `release/**` and pinned the `audit-system-migration.py` baseline to a stable SHA.
- Performance: bounded icon cache, fixed MainApplication memory callbacks, lazy catalog/resolver initialization, and reduced per-hook allocations on statusbar, notification, network speed, battery, clock, and launcher hot paths.

## Status

**Locally-signed candidate, pending real-device and LSPosed log validation.** No Git tag or GitHub Release is created.

## Release gates

- `check-invariants.py`: PASS (136 files, 0 violations)
- `audit-system-migration.py`: PASS (124/124 baseline methods, 119/119 direct calls, 0 facade calls)
- `audit-architecture.py`: PASS
- `audit-prefs.py`: PASS (schema/code/xml aligned, no duplicates; many legacy preference keys outside schema is a known state)
- `audit-canary-sequence.py`: PASS
- `audit-catalog-contracts.py`: PASS
- `:app:test`: PASS
- `:app:lintDebug`, `:app:lintRelease`: PASS
- `:app:lintVitalRelease`: SKIPPED (no vital variant in current configuration)
- `:app:assembleDebug`, `:app:minifyReleaseWithR8`, `:app:assembleRelease`: PASS
- zipalign: PASS (aligned during `packageRelease`)
- APK Signature Scheme v2: PASS
- Legacy Xposed API DEX scan: PASS

## Artifact

- File: `CustoMIUIzer-A13-r13.8.5.apk`
- Size: `2,830,726` bytes
- SHA-256: `98355766E3D13EDBFCBABD0021EFEF574A2427D19EB34A6A2CF07F394A309FE1`
- Certificate SHA-256: `C0EFF2DC4E662717195490DA78B12A984C6F2E6BD38ACF4EDAD14D53E3D22E70`
- Certificate DN: `CN=CustoMIUIzer A14, OU=Release, O=tomthenpc, C=CN`
- applicationId: `tv.withaibuild.customiuizer.r13`
- versionCode: `130`
- versionName: `r13.8.5`

## Validation boundary

This is a locally-signed candidate; compilation, lint, R8, and signature are static checks and cannot replace device/ROM behavior validation. Disable the affected feature and provide complete LSPosed logs for any compatibility issue.
