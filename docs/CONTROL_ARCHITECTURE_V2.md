# A13 开发控制架构 v2

本架构在一次破坏式迁移中替换了旧文档体系。旧 `docs/`、`tasks/` 和根目录工程文档
没有归档，也不再具有控制权。

核心变化：

- ChatGPT 负责代码检查、任务合同和最终审查；
- Devin 负责实现、验证和构建；
- 一个目标对应一个任务；
- Review 不再是独立阶段；
- HEAD 自动记录；
- A13/A14 分开规划和实现；
- 功能推进高于无限审计；
- 自动门禁代替重复文字证明。

Git 历史是旧文档唯一回退来源。
