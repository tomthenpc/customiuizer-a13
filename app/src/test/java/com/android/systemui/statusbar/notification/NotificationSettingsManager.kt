package com.android.systemui.statusbar.notification

/**
 * Stub without USE_WHITE_LISTS so the static field write must fail-open
 * instead of aborting later FilterHelper hooks.
 */
object NotificationSettingsManager
